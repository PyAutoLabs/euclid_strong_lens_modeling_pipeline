[pytest]
testpaths = tests
markers =
    slow: a test that runs a real (non-TEST_MODE) fit. Excluded from the fast
        unit job with `-m "not slow"` and run in its own CI job with `-m slow`,
        so "the pipeline stopped writing latents" is distinguishable from "a
        latent value is wrong".
